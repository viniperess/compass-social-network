const HOST = "http://localhost";
const PORT = 3001;


export const PATH = {
    USERS: `${HOST}:${PORT}/api/v1/users`,
    POSTS: `${HOST}:${PORT}/api/v1/posts`,
    LOGIN: `${HOST}:${PORT}/api/v1/login`,
    REGISTER: `${HOST}:${PORT}/api/v1/users`
}